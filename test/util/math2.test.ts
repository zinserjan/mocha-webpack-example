import { add, subtract } from '../../src/util/math2';

describe('math2', function () {

  it('add()', function () {
    add(1, 2);
  });

  it('subtract()', function () {
    subtract(2, 1);
  });
});
